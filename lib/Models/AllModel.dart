class LoginModel {
  String? email, password;

  LoginModel({
    this.email,
    this.password,
  });
}

class TitleModel {
  String? image, name, routes;

  TitleModel({
    this.image,
    this.routes,
    this.name,
  });
}
