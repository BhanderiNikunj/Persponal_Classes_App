class HomeWorkModel {
  String? title, subject, dueDate, key;
  int? std, check;

  HomeWorkModel({
    this.title,
    this.subject,
    this.dueDate,
    this.key,
    this.std,
    this.check,
  });
}
